{
  "scripts": {
    "build": "esbuild app.ts --bundle --minify --outfile=out.js"
  },
  "devDependencies": {
    "@types/node": "^14.14.41",
    "esbuild": "^0.11.12",
    "typescript": "^4.2.4"
  }
}
